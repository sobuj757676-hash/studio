
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the entire database.
    // In a production app, you should secure your data with more granular rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
